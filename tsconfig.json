/* Dev configuration */
{
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "allowSyntheticDefaultImports": true, // Used to import assert in unit tests
  },
  /* During dev, src are included so they are also modified during refactorings. */
  "include": ["src/**/*.ts", "test/**/*.ts"],
}
